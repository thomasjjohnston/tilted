services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: tilted
      POSTGRES_PASSWORD: tilted
      POSTGRES_DB: tilted
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
