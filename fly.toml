app = 'tilted-server'
primary_region = 'iad'

[build]
  dockerfile = "apps/server/Dockerfile"

[deploy]
  # Run pending Drizzle migrations in a one-off VM before promoting the
  # new image. Blocks the deploy if migrations fail, so prod never boots
  # against a stale schema.
  release_command = "node apps/server/dist/db/migrate.js"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1

[checks]
  [checks.health]
    port = 3000
    type = "http"
    interval = "15s"
    timeout = "2s"
    grace_period = "5s"
    method = "GET"
    path = "/healthz"
